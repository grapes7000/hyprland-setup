# bin

`shortcuts` — prints the keybind + command cheat sheet (also bound to
Super+Shift+? in Hyprland). Copy to `~/.local/bin/` and `chmod +x`.

The theme tools (`theme`, `theme-new`, `theme-menu`, `wallgen`) live in the
separate [`themes`](../../themes) repo.
