# Safe defaults for variables used across config files.
# generated/theme.conf loads after this and overrides them.
$group_active = rgba(cba6f7ff)
$group_inactive = rgba(313244aa)
$group_locked = rgba(f38ba8ff)
$group_text = rgb(cdd6f4)
