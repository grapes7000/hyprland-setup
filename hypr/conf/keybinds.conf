$mod  = SUPER
$term = kitty
$menu = ~/.local/bin/wofi-singleton --show drun --style ~/.config/wofi/style.css

# --- apps / session ---
bind = $mod, Return, exec, $term
bind = $mod, Space,  exec, $menu
bind = $mod, B,      exec, rofi-rbw

# --- wofi: click outside, or same key again, to close ---
submap = wofi
bind = , mouse:272, exec, pkill -x wofi
bind = , mouse:273, exec, pkill -x wofi
bind = $mod, Space, exec, $menu
bind = $mod, T, exec, ~/.local/bin/theme-menu
bind = $mod SHIFT, P, exec, ~/.config/bin/power-menu
bind = $mod SHIFT, W, exec, ~/.config/bin/workspace-switcher
submap = reset
bind = $mod, Q,      killactive,
bind = $mod SHIFT, E, exit,
bind = $mod, F,      fullscreen,
bind = $mod, V,      togglefloating,
bind = $mod, X,      layoutmsg, togglesplit
bind = $mod, P,      pseudo,

# --- focus (vim h/j/k/l + arrows) ---
bind = $mod, h,     movefocus, l
bind = $mod, l,     movefocus, r
bind = $mod, k,     movefocus, u
bind = $mod, j,     movefocus, d
bind = $mod, left,  movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up,    movefocus, u
bind = $mod, down,  movefocus, d

# --- move window (Shift + vim + arrows) ---
bind = $mod SHIFT, h,     movewindow, l
bind = $mod SHIFT, l,     movewindow, r
bind = $mod SHIFT, k,     movewindow, u
bind = $mod SHIFT, j,     movewindow, d
bind = $mod SHIFT, left,  movewindow, l
bind = $mod SHIFT, right, movewindow, r
bind = $mod SHIFT, up,    movewindow, u
bind = $mod SHIFT, down,  movewindow, d

# --- resize (Ctrl + vim) ---
bind = $mod CTRL, h, resizeactive, -40 0
bind = $mod CTRL, l, resizeactive,  40 0
bind = $mod CTRL, k, resizeactive,  0 -40
bind = $mod CTRL, j, resizeactive,  0  40

# --- workspaces ---
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up,   workspace, e-1

# --- theme picker (Super+T) ---
bind = $mod, T, exec, ~/.local/bin/theme-menu

# --- workspace switcher (Super+Shift+W) ---
bind = $mod SHIFT, W, exec, ~/.config/bin/workspace-switcher

# --- power menu (Super+Shift+P) ---
bind = $mod SHIFT, P, exec, ~/.config/bin/power-menu

# --- quick notes (Super+Shift+N) ---
bind = $mod SHIFT, N, exec, ~/.config/bin/quick-note

# --- notifications (Super+N / Super+Ctrl+N) ---
bind = $mod, N, exec, ~/.config/bin/notification-center
bind = $mod CTRL, N, exec, dunstctl history-pop

# --- grouped windows ---
bind = $mod, G, togglegroup,
bind = $mod ALT, left, changegroupactive, b
bind = $mod ALT, right, changegroupactive, f
bind = $mod SHIFT, G, lockactivegroup, toggle

# --- keybind menu (Super+Shift+/) ---
bind = $mod SHIFT, slash, exec, ~/.config/bin/keybind-menu

# --- screenshots ---
bind = , Print,    exec, grim -g "$(slurp)" - | wl-copy
bind = $mod, S,    exec, grim -g "$(slurp)" - | wl-copy

# --- media / brightness (repeat while held) ---
bindel = , XF86AudioRaiseVolume,  exec, pamixer -i 5
bindel = , XF86AudioLowerVolume,  exec, pamixer -d 5
bindl  = , XF86AudioMute,         exec, pamixer -t
bindl  = , XF86AudioPlay,         exec, playerctl play-pause
bindl  = , XF86AudioNext,         exec, playerctl next
bindl  = , XF86AudioPrev,         exec, playerctl previous
bindel = , XF86MonBrightnessUp,   exec, brightnessctl set 5%+
bindel = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

# --- mouse drag ---
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow
